# mig-assets (temporary)

Hospital HTML migration - temporary image host.
Images are fetched once by the target CMS converter and rehosted internally.
This repository is disposable and can be deleted after conversion.

